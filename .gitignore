# PyCharm config folder; not needed in project
.idea/
# Local Python virtual environment; not needed in project
venv/
# Python binary files (.pyc); not needed in project
__pycache__/
# Website database; not needed because generated by reset_everything.py
db.sqlite3
# Migrations files; not needed because generated by reset_everything.py
# We'll need them in production, since at that point we can't wipe the database every time something goes wrong,
#  but in development we don't need them.
**/migrations/**
# Make sure __init__.py stays
!**/migrations/__init__.py
# Config file for macOS Finder; security issue if made public + not needed in project
.DS_Store
